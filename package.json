{
  "private": true,
  "name": "proposal-function-implementation-hiding",
  "description": "Function implementation hiding proposal for JavaScript (build setup only)",
  "license": "MIT",
  "repository": "domenic/proposal-function-implementation-hiding",
  "scripts": {
    "prebuild": "mkdir out || exit 0",
    "build": "ecmarkup spec.html out/index.html",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "ecmarkup": "^3.16.0"
  }
}
